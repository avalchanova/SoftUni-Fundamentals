function addAndSubstract(a, b, c) {

    let sum = add(a, b);
    let finalRes;
    console.log(substract(c));

    function add(a, b) {
        return (a + b);
    }

    function substract(c) {
        return finalRes = sum - c;
    }

}
addAndSubstract(23, 6, 10);